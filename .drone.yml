image: scrapinghub

script:
  - build_docker_image --build-arg BASE_IMAGE=i.scrapinghub.com/crawlera/docker-erlang-otp/elixir/keepcase:04d6990-slim --build-arg DISABLE_SYSLOG=1 --build-arg=DISABLE_SSH=0 --rm image  # SSH is needed to run test
  - |
    set -ex
    NAME=${APPIMAGE/:*/} \
    VERSION=${APPIMAGE/*:/} \
    make test
  - push_docker_image

